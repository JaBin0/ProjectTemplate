#pragma once

// IDs of build in shaders
namespace SHADER_ID {
    enum SHADER_ID {
        SHADER_ERROR = 0,
        DEFAULT,
        COUNT
    };
};

namespace MESH_ID {
    enum MESH_ID {
        CUBE = 0,
        COUNT
    };
};